<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="10dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingBottom="0dp"
    android:background="@android:color/white"
    tools:context=".MainActivity">

    <!-- Saluto con il nome dell'account -->
    <TextView
        android:id="@+id/tv_greeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ciao, NomeAccount!"
        android:textSize="30sp"
        android:layout_marginBottom="30dp"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="30dp"
        android:id="@+id/totalespeso"
        android:layout_below="@id/tv_greeting"
        android:text="Questo mese hai speso:"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="30dp"
        android:text="1000$"
        android:layout_below="@id/totalespeso"
        android:id="@+id/totalespesovalue"/>

    <!-- Barra orizzontale divisa per categorie di spesa -->
    <LinearLayout
        android:id="@+id/category_bar"
        android:layout_width="match_parent"
        android:layout_height="24dp"
        android:layout_below="@id/totalespesovalue"
        android:layout_marginTop="15dp"
        android:orientation="horizontal">

        <!-- Esempio di segmenti colorati -->
        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/purple"/>

        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="@android:color/holo_green_light"/>

        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@android:color/holo_red_light"/>
    </LinearLayout>

    <!-- ListView per gli elementi dal database -->
    <ListView
        android:id="@+id/lv_expense_items"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@id/category_bar"
        android:scrollbars="vertical"
        android:layout_marginTop="25dp"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Pulsante fisso per aggiungere una nuova spesa -->
    <Button
        android:id="@+id/btn_add_expense"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aggiungi spesa"
        android:background="@drawable/button_background"
        android:textSize="16sp"
        android:layout_above="@+id/menubar"
        android:layout_margin="10dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/menubar"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/home"
            android:background="@color/white"/>
        <ImageButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/layers"
            android:background="@color/white"/>
        <ImageButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/book"
            android:background="@color/white"/>
        <ImageButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/user"
            android:background="@color/white"/>
    </LinearLayout>
</RelativeLayout>
